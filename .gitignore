# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/

# 환경/비밀
.env

# IDE
.vscode/
.idea/

# TTS 생성 음원 (스크립트로 재생성 가능)
experiments/tts_out/

# 육성 녹음 (개인 음성 데이터 — 로컬 보관. manifest.csv는 추적)
experiments/recordings/*.wav
experiments/recordings/*.mp3
experiments/recordings/*.m4a
experiments/recordings/*.webm
experiments/recordings/*.ogg

# 데모 영상 (대용량 — 로컬 보관, 배포는 링크로. 대본 md는 추적)
docs/demo/*.mp4

# 런타임 데이터 전체 (DB·TTS 캐시·업로드 녹음)
# data/aihub/ — AI Hub 데이터셋은 이용 약관상 재배포 금지: 반드시 이 폴더 아래 보관
data/

# AI Hub 원본 라벨을 재포장한 매니페스트 (재배포 금지 — 스크립트로 재생성 가능)
# 집계 리포트(aihub_inventory.md)는 숫자뿐이라 추적 대상.
experiments/results/aihub_manifest.csv
*.db
*.sqlite3
uploads/


*.zip